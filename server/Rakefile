namespace :jekyll do
    desc 'start jekyll serve'
    task :serve do
        #FileUtils.cp 'public/js/config-dev.js', 'public/js/config.js'
        sh "bundle exec jekyll serve --port=9090 --host=0.0.0.0"
    end
end